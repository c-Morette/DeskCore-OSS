namespace DeskCore.Shared.Enums;

/// <summary>
/// Prioridade do ticket. Valores em inglês (código);
/// a tradução PT-BR é responsabilidade da camada de apresentação.
/// </summary>
public enum TicketPriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3
}
